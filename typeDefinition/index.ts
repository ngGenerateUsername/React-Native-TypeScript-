import { StackNavigationProp } from "@react-navigation/stack";

export type stackNavigationProp = {
    navigation:StackNavigationProp<any>,
    // route:NavigatorScreenParams<any>; //passing props with navigation
}